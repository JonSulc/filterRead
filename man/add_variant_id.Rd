% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_versioned_columns.R
\name{add_variant_id}
\alias{add_variant_id}
\title{Construct variant_id from chr / pos / ref / alt}
\usage{
add_variant_id(dt, build = NULL, overwrite = FALSE)
}
\arguments{
\item{dt}{A data.table with `chr`, `pos`, `ref`, `alt` columns.}

\item{build}{Build name used in the suffix. Defaults to `build(dt)`.}

\item{overwrite}{If FALSE (default) and `variant_id` already exists,
leave it alone. If TRUE, recompute.}
}
\value{
`dt` (invisibly).
}
\description{
Adds a `variant_id` column shaped as `chr_pos_ref_alt_<build>`
(e.g. `chr1_12345_A_G_b38`). Rows where any of `chr`, `pos`, `ref`, or `alt`
is NA get `NA` for `variant_id`. Modifies `dt` by reference.
}

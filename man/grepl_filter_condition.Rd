% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_condition_conversion.R
\name{grepl_filter_condition}
\alias{grepl_filter_condition}
\title{Build awk regex-match condition for grepl}
\usage{
grepl_filter_condition(pattern, column_name)
}
\arguments{
\item{pattern}{Length-1 character vector with the regex pattern.}

\item{column_name}{Awk column reference (e.g. `"$3"`).}
}
\value{
List with `condition` element: `"$3 ~ /pattern/"`.
}
\description{
Translates `grepl(pattern, column)` into `$N ~ /pattern/` for awk.
The pattern must be a length-1 character string. Only `/` is
escaped (it is the awk regex-literal delimiter); the rest of the
pattern is passed through, so the caller writes awk-compatible
POSIX regex syntax.
}
\keyword{internal}

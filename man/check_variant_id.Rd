% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_versioned_columns.R
\name{check_variant_id}
\alias{check_variant_id}
\title{Validate a variant_id column against coordinates and build}
\usage{
check_variant_id(x, build = NULL)
}
\arguments{
\item{x}{A data.table with a `variant_id` column.}

\item{build}{Build the ids are expected to embed. Defaults to `build(x)`.}
}
\value{
`x`, invisibly.
}
\description{
For every row whose `variant_id` parses (see [parse_variant_id()]), each
non-NA `chr`/`pos`/`ref`/`alt` value must equal the decoded component and
the embedded build must equal `build` (compared after canonicalizing known
synonyms). NA coordinate cells and rows with an unparseable `variant_id`
impose no constraint. Errors listing the offending rows; otherwise returns
`x` invisibly.
}

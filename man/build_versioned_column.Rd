% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_versioned_columns.R
\name{build_versioned_column}
\alias{build_versioned_column}
\title{Read a coordinate or allele column as recorded for a build}
\usage{
build_versioned_column(dt, base, build)
}
\arguments{
\item{dt}{A data.table.}

\item{base}{Unsuffixed column name (e.g. "chr", "pos", "ref", "alt").}

\item{build}{Build whose values to read.}
}
\value{
The column vector, or `NULL` if not recorded.
}
\description{
Returns column `base` holding `build`'s values: the `<base>_<build>`
suffixed column when present, the canonical `base` column when `build` is
`dt`'s current build, else `NULL`. Read-side counterpart to [record_build()].
}
\keyword{internal}

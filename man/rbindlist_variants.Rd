% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variants_combine.R
\name{rbindlist_variants}
\alias{rbindlist_variants}
\title{Combine variants row-wise into a single variants}
\usage{
rbindlist_variants(
  x,
  target = NULL,
  source = "best",
  multi_match = "first",
  fill = TRUE,
  ...
)
}
\arguments{
\item{x}{A list of `variants` / data.tables.}

\item{target}{Build to express the result in; defaults to the first input's.}

\item{source, multi_match}{Forwarded to [liftover.variants()].}

\item{fill}{Forwarded to [data.table::rbindlist()]; defaults to `TRUE` so
ragged provenance/domain columns reconcile (the lone divergence from
`rbindlist`, whose default is `FALSE`).}

\item{...}{Other [data.table::rbindlist()] options (`idcol`, `use.names`).}
}
\value{
A `variants` in `target`.
}
\description{
Row-binds a list of `variants` (or data.tables coercible with
[as_variants()]). Inputs in a build other than `target` are lifted to it;
`target` defaults to the first input's build. Per-row `defining_build` is
carried through unchanged. An empty `x` returns a 0-row `variants` in
`target`, which must then be supplied (there is no input build to inherit).
}

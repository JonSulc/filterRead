% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genomic_regions_class.R
\name{genomic_regions}
\alias{genomic_regions}
\title{Construct genomic regions for positional filtering}
\usage{
genomic_regions(
  chr = character(),
  start = integer(),
  end = integer(),
  build = NULL,
  chr_prefix = "chr",
  merge_contiguous = TRUE,
  include = TRUE
)
}
\arguments{
\item{chr}{Character vector of chromosome names (NA = any chromosome).}

\item{start}{Integer vector of start positions (NA = from beginning).}

\item{end}{Integer vector of end positions (NA = to end).}

\item{build}{Reference build the coordinates are expressed in ("b36",
"b37", "b38", or a synonym). Optional at construction, but positional
filtering against a build-bearing file requires it (see
[as_genomic_regions()] and the file-interface `[` method).}

\item{chr_prefix}{Prefix to ensure on chromosome names (default "chr").}

\item{merge_contiguous}{If TRUE, merge overlapping/adjacent regions.}

\item{include}{If TRUE the regions are included; if FALSE they are
excluded (the complement).}
}
\value{
A `genomic_regions` object.
}
\description{
Builds a `genomic_regions` object — a set of `(chr, start, end)`
coordinate ranges in one reference build — from parallel vectors. Pass
the result as a `[` subscript to a `file_interface` to filter its rows by
position: `finterface[genomic_regions(...)]`. To build regions from an
existing `data.table` of `chr`/`start`/`end`, use [as_genomic_regions()].
}
\examples{
genomic_regions(
  chr = c("1", "2"),
  start = c(1000L, 5000L),
  end = c(2000L, 6000L),
  build = "b38"
)
}
\seealso{
[as_genomic_regions()] to coerce an existing table.
}
